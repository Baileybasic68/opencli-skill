# Agent Prompts

These are realistic prompts that should naturally trigger the OpenCLI skill.

## Discovery

- Use OpenCLI to check what site adapters are available on this machine.
- Inspect the installed OpenCLI surface and tell me whether Bilibili, Zhihu, and Codex adapters exist.
- Use OpenCLI to troubleshoot why my browser-backed commands are returning empty data.

## Browser-Backed Sites

- Use OpenCLI to fetch the top 10 Bilibili hot videos as JSON.
- Pull Zhihu search results for "Android 性能优化" through OpenCLI and summarize the top themes.
- Use OpenCLI to search Xiaohongshu for "AI Agent" and return the first 10 results in YAML.
- Export this Weixin article through OpenCLI and save it locally as Markdown.

## Desktop Adapters

- Use OpenCLI to inspect the current Codex desktop conversation and summarize it.
- Use OpenCLI to read Cursor's current conversation and tell me what model it is using.
- Use OpenCLI to export the current Codex thread to Markdown.

## Downloads

- Download this Bilibili video with OpenCLI into `./bilibili`.
- Use OpenCLI to export a Zhihu article and download all images.
- Download the media from this Xiaohongshu note through OpenCLI.

## Adapter Development

- Explore this site with OpenCLI and generate an adapter for its hot list.
- Use OpenCLI cascade to probe the authentication strategy for this endpoint.

## Chinese Prompts

- 用 OpenCLI 拉一下 B 站热榜，返回 JSON。
- 用 OpenCLI 搜索知乎里的“Android 性能优化”，先给我前 10 条。
- 用 OpenCLI 读取当前 Codex 桌面会话并总结最近进度。
- 用 OpenCLI 检查为什么小红书命令拿不到数据。
